sig nqueens.
/*
    nqueens +N -L holds iff L is an nqueens solution to a board of NxN.
    A solution is a list of N numbers, all of them distinct, where the i-th number is the vertical position (column)
    of the queen on the i-th row.
*/
type nqueens int -> list int -> o.
end